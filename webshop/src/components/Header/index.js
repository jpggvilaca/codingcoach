import React from 'react';

import { InnerContainer } from './styles';

const Header = () => {
  return (
    <header>
      <InnerContainer>
        Header
      </InnerContainer>
    </header>
  );
}

export default Header;
